meta:
  environment: node
  toolchain:
    name: npm
    version: 18
build:
  artifacts:
    '*': /
  command: ls
run:
  persistenceMount: /data
  containerPort: 80


# meta:
#   environment: node
#   toolchain:
#     name: npm
#     version: 18
# build:
#   artifacts:
#     '*': /
# run:
#   scriptName: ./amvera.yml
#   persistenceMount: /data
#   containerPort: 80
