meta:
  environment: node
  toolchain:
    name: npm
    version: 18
build:
  artifacts:
    '*': /
  command: npm install && npm run build && npm run start:prod
run:
  persistenceMount: /data
  containerPort: 80
